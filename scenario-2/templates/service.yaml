apiVersion: v1
kind: Service
metadata:
  name: {{ include "scenario-2.fullname" . }}
  labels:
    {{- include "scenario-2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scenario-2.selectorLabels" . | nindent 4 }}
