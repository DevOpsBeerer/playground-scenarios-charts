apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scenario-2.fullname" . }}-test-connection"
  labels:
    {{- include "scenario-2.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scenario-2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
